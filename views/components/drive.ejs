<section id="drive">
	<% if (!subdrive.subdirs || subdrive.subdirs.length === 0) { %>
		<p>your drive is empty.</p>
	<% } else { %>
		<p>drive loaded... hmm, it should go here.</p>
	<% } %>
</section>

